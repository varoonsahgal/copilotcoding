{
  "name": "copilot-coding-agent-incident-lab",
  "version": "1.0.0",
  "description": "Lab for GitHub Copilot coding agent: incident to fix",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
